---
import Layouts from "@layouts/Layouts.astro";

import { Hero, EventsList } from "@components/resources";

import { getEvents } from "@utils/prismic";
import { eventsConfig } from "@utils/constants";

import HeroImage from "@assets/images/resources/events.webp";

const eventsApiData = await getEvents({ page: 1, pageSize: eventsConfig.maxRecordsPerPage });
---

<Layouts
  title="Events • Resources | Complete Farmer"
  description="We are providing cutting-edge technological farming protocols and innovations with a unique business model and logistics that are revolutionizing..."
>
  <Hero image={HeroImage} title="Events" />
  <EventsList {eventsApiData} client:idle />
</Layouts>
