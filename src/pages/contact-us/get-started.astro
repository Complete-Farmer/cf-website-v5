---
import Layouts from "@layouts/Layouts.astro";

// import FAQContent from "@components/contact-us/FAQContent";

import { ChevronIcon } from "@assets/icons";
// import { getContactUsFaqs } from "@utils/prismic";
// import { maxRecordsPerPage } from "@utils/constants";
// import { formatDateWithCommas } from "@utils/functions";

// const faqsApiData = await getContactUsFaqs({
//   page: 1,
//   pageSize: maxRecordsPerPage,
// });

// const faqs = faqsApiData.results.map((item) => ({
//   ...item,
//   date: formatDateWithCommas(item.last_publication_date),
//   question: item.data.question[0].text,
//   answer: item.data.answer[0].text,
//   uid: item.uid,
// }));
---

<Layouts
  title="faQs | Complete Farmer"
  description="We are providing cutting-edge technological farming protocols and innovations with a unique business model and logistics that are revolutionizing..."
>
  <div class="bg-grower-400 text-white fixed w-full h-14 overflow-hidden z-10">
    <div
      class="max-w-7xl mx-auto px-4 z-10 sm:px-8 py-4 flex justify-start items-center space-x-2"
    >
      <a href="/contact-us" class="font-bold underline">
        <span>Contact us</span>
      </a>
      <ChevronIcon className="-rotate-90" />
      <span>Get started with Complete Farmer</span>
    </div>
  </div>

  <!-- <FAQContent {faqs} client:visible/> -->
</Layouts>
